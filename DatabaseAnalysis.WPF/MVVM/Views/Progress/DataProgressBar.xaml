<Window x:Class="DatabaseAnalysis.WPF.MVVM.Views.Progress.DataProgressBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:progress="clr-namespace:DatabaseAnalysis.WPF.MVVM.ViewModels.Progress" 
        d:DataContext="{d:DesignInstance Type=progress:DataProgressViewModel}"
        mc:Ignorable="d"
        Title="DataProgressBar" Height="50" Width="300" 
        WindowStyle="None" WindowStartupLocation="CenterScreen" >
    <Grid>
        <ProgressBar Name="pbStatus" Minimum="0" Maximum="100"
                     Value="{Binding Path=ValueBar, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="{Binding ElementName=pbStatus, Path=Value, StringFormat={}{0:0}%}" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</Window>
